import React from 'react';

let Signin = React.createClass({

  render() {
  	return (
      <div>
        <h2>sss</h2>
        {this.props.children}
      </div>
    )
  }
});

export default Signin;

